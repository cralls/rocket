<?php
/** @var \Rss\CustomDesigns\Block\Adminhtml\Production\Edit\Form $block */
$model = $block->getModel();
$data  = $model ? $model->getData() : [];

function e($v) {
    return htmlspecialchars((string)$v, ENT_QUOTES);
}
function checked($key, $val, $data) {
    return ((string)($data[$key] ?? '') === (string)$val) ? 'checked' : '';
}
?>

<form action="<?= $block->getUrl('customdesigns/production/save') ?>"
      method="post"
      class="admin__scope-old">

    <input type="hidden" name="form_key" value="<?= $block->getFormKey() ?>" />

    <?php if (!empty($data['entity_id'])): ?>
        <input type="hidden" name="entity_id" value="<?= (int)$data['entity_id'] ?>" />
    <?php endif; ?>

    <fieldset class="admin__fieldset">
        <legend class="admin__legend">
            <span><?= __('Production Request') ?></span>
        </legend>

        <div class="admin__field">
            <label class="admin__field-label"><?= __('Customer Name') ?></label>
            <div class="admin__field-control">
                <input type="text" name="customer_name" class="admin__control-text"
                       value="<?= e($data['customer_name'] ?? '') ?>" required>
            </div>
        </div>

        <div class="admin__field">
            <label class="admin__field-label"><?= __('Sales') ?></label>
            <div class="admin__field-control">
                <input type="text" name="sales_name" class="admin__control-text"
                       value="<?= e($data['sales_name'] ?? '') ?>" required>
            </div>
        </div>

        <div class="admin__field">
            <label class="admin__field-label"><?= __('Sales Email') ?></label>
            <div class="admin__field-control">
                <input type="email" name="sales_email" class="admin__control-text"
                       value="<?= e($data['sales_email'] ?? '') ?>" required>
            </div>
        </div>

        <div class="admin__field">
            <label class="admin__field-label"><?= __('VIP Customer') ?></label>
            <div class="admin__field-control">
                <label>
                    <input type="radio" name="is_vip" value="1"
                        <?= checked('is_vip', 1, $data) ?>> <?= __('Yes') ?>
                </label><br>
                <label>
                    <input type="radio" name="is_vip" value="0"
                        <?= checked('is_vip', 0, $data) ?>> <?= __('No') ?>
                </label>
            </div>
        </div>

        <div class="admin__field">
            <label class="admin__field-label"><?= __('Delivery Request Date') ?></label>
            <div class="admin__field-control">
                <input type="date" name="delivery_request_date" class="admin__control-text"
                       value="<?= e($data['delivery_request_date'] ?? '') ?>" required>
            </div>
        </div>

        <div class="admin__field">
            <label class="admin__field-label"><?= __('Fit and Sizing') ?></label>
            <div class="admin__field-control">
                <?php foreach ([
                    'Slim Fit',
                    'Regular Fit',
                    'Relax Fit',
                    'Size Up',
                    'Size Down',
                    'No Special Request'
                ] as $opt): ?>
                    <label>
                        <input type="radio" name="fit_and_sizing" value="<?= $opt ?>"
                            <?= checked('fit_and_sizing', $opt, $data) ?>>
                        <?= $opt ?>
                    </label><br>
                <?php endforeach; ?>
            </div>
        </div>

        <div class="admin__field">
            <label class="admin__field-label"><?= __('Shipping Method') ?></label>
            <div class="admin__field-control">
                <label>
                    <input type="radio" name="shipping_method" value="Economy"
                        <?= checked('shipping_method', 'Economy', $data) ?>> <?= __('Economy') ?>
                </label><br>
                <label>
                    <input type="radio" name="shipping_method" value="Priority"
                        <?= checked('shipping_method', 'Priority', $data) ?>> <?= __('Priority') ?>
                </label>
            </div>
        </div>

        <div class="admin__field">
            <label class="admin__field-label"><?= __('Shipment Type') ?></label>
            <div class="admin__field-control">
                <?php foreach ([
                    'Bulk Shipment to One Address',
                    'Team Portal USA',
                    'Individual Shipment to team portal customer',
                    'Other'
                ] as $opt): ?>
                    <label>
                        <input type="radio" name="shipment_type" value="<?= $opt ?>"
                            <?= checked('shipment_type', $opt, $data) ?>>
                        <?= $opt ?>
                    </label><br>
                <?php endforeach; ?>
            </div>
        </div>

        <div class="admin__field">
            <label class="admin__field-label"><?= __('Special Instructions') ?></label>
            <div class="admin__field-control">
                <textarea name="special_instructions"
                          class="admin__control-textarea"><?= e($data['special_instructions'] ?? '') ?></textarea>
            </div>
        </div>

    </fieldset>

    <div class="page-actions">
        <button type="submit" class="action-primary">
            <span><?= __('Save Production Request') ?></span>
        </button>
    </div>
</form>
